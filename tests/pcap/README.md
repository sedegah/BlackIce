# PCAP Fixture Layout

Place deterministic replay captures here:

- `normal_web.pcap` for baseline windows (expect low anomaly)
- `syn_flood.pcap` for SYN flood windows (expect high anomaly)
- `mixed_attack.pcap` for mixed traffic windows (expect score spikes)

These files are intentionally not committed to keep repository size small.
